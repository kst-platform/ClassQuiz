# КСТ.Квиз — ход работы

Форк [ClassQuiz](https://github.com/mawoka-myblock/classquiz) (MPL-2.0) под
ГБПОУ КСТ. Полный план и обоснование выбора движка — скил `svoy-kahut`
(`~/.claude/skills/svoy-kahut/`, файл `references/vybor-dvizhka.md`) и
исходный план `~/.claude/plans/adaptive-plotting-puffin.md` (сессия
2026-09-22). Этот файл — рабочий чек-лист, живёт в репозитории, чтобы не
потеряться между сессиями.

Ветки: `master` синхронизируется с `upstream` (mawoka-myblock/classquiz),
вся работа — в ветке `kst`.

## Фаза 0 — форк и локальный подъём — ✅ готово
- [x] Принято приглашение в организацию `kst-platform`.
- [x] Форк `kst-platform/ClassQuiz`, клон в `~/Projects/_tools/kst-kviz`.
- [x] `upstream` remote добавлен для будущих обновлений.
- [x] **Реально поднято и проверено 22.09.2026** — не через Docker (на этой
      машине нет прав администратора, Docker недоступен), а напрямую:
      Postgres 17 (отдельный одноразовый кластер на порту 5433, не трогая
      существующий сервис), Redis (портативная сборка
      `taizod1024/redis-windows-fork`, user-scope через winget), Meilisearch
      (бинарник с GitHub releases), бэкенд через `pipenv sync` + `uvicorn`.
      **Важно на будущее**: ставить зависимости строго `pipenv sync` (не
      `--skip-lock`) — иначе резолвятся более новые alembic/ormar/databases/
      sqlalchemy, несовместимые со старыми миграциями апстрима (raw SQL без
      `text()`). Также нужен `python-magic-bin` (не в Pipfile, но без него
      импорт `classquiz.routers.eximport` зависает на Windows намертво,
      без ошибки — самая коварная находка сессии).

## Фаза 1 — бренд «КСТ.Квиз» — 🔶 частично
- [x] `frontend/tailwind.config.cjs` — токены палитры `kst.*` + шрифт Montserrat.
- [x] `frontend/src/app.html` — заголовок/meta «КСТ.Квиз», `lang="ru"`,
      подключение Montserrat, тема `#2F678C`, убран старый плейсхолдер
      `window.plausible`.
- [x] `frontend/src/routes/+layout.svelte` — глобальный фон убран с зелёного
      ClassQuiz на `#F5F6F6`/`#26333C` (light/dark).
- [x] `frontend/src/app.css` — hover-цвета и `normal-background` переведены
      на палитру КСТ.
- [x] `frontend/src/lib/brand/logo.svelte` — новый компонент: иконка +
      текстовый wordmark «КСТ.Квиз», без файлов-логотипов КСТ/МО.
- [x] `frontend/src/lib/navbar.svelte` — логотип заменён на компонент,
      убраны донат-кнопка и ссылка на GitHub апстрима (десктоп и мобильная
      версии).
- [x] `frontend/src/lib/footer.svelte` — переписан целиком: без донатов,
      Mastodon и самопиара автора апстрима.
- [x] Удалены `logo.png`, `logo.png.license` (не используются нигде кроме
      старого README).
- [x] `README.md` переписан под форк.

**Не сделано, следующий шаг Фазы 1:**
- [ ] Пройти оставшиеся ~75 файлов с упоминанием `ClassQuiz` (список получен
      через `grep -rl ClassQuiz frontend/src`) — это в основном:
      `frontend/src/lib/i18n/locales/*.json` (нужно поправить как минимум
      `ru.json` и `en.json` — найти ключ с названием бренда и сменить на
      «КСТ.Квиз»; остальные ~30 языков можно не трогать или удалить, если
      многоязычность не нужна колледжу — решить отдельно),
      `frontend/src/routes/+page.svelte` (лендинг), `frontend/src/routes/docs/*`
      (self-host/roadmap/attribution/tos/privacy-policy/pow — либо переписать
      под КСТ, либо удалить как нерелевантные внутреннему инструменту),
      `frontend/src/routes/account/*` (страницы входа/регистрации — просто
      упоминания бренда в тексте, дизайн уже наследует токены), favicon и
      `apple-touch-icon`/`site.webmanifest` в `frontend/static/` (сейчас это
      старые иконки ClassQuiz — заменить на иконку из `logo.svelte`).
- [ ] Дать пройти автоматическому линтеру/сборке (`pnpm build`) после правок —
      не запускалось в этой сессии (нет Node/pnpm на машине без прав
      администратора; собирать на стенде или через Docker).

## Фаза 2 — учётки преподавателей — ✅ бэкенд и панель готовы, деплой не проверен

Расширено по прямой просьбе пользователя 22.09.2026: админ умеет не только
одобрять самостоятельную регистрацию, но и **создавать учётки сам**,
**менять пароль** любой учётке и **удалять** учётку — удаление и смена
пароля только через повторный ввод **собственного пароля админа**.

- [x] Миграция `2a95fea0c125_added_admin_approval_field.py`: `users.approved`
      (`NOT NULL DEFAULT false`) — отдельно от `verified`.
- [x] **Важная находка и правка**: `get_admin_user` в `classquiz/auth.py`
      раньше считал админом "первую по дате создания строку в users" —
      права зависели от порядка вставки, не от конфигурации, их нельзя было
      ни проверить, ни сменить без прямого доступа к базе. Переписано на
      явный список `settings.admins` (переменная окружения `ADMINS`), по
      аналогии с уже существующим `settings.mods`/`get_current_moderator`.
- [x] `classquiz/oauth/authenticate_user.py`: `log_user_in` и
      `rememberme_check` — единая точка для всех способов входа (пароль,
      passkey, TOTP, backup-код) — отклоняют вход с 403, пока
      `user.approved is False`.
- [x] `classquiz/routers/admin.py`: `GET /users`, `GET /users/pending`,
      `POST /users/{id}/approve`, `POST /users` (создание админом, сразу
      `approved=True`), `POST /users/{id}/reset-password`.
- [x] `DELETE /user/id|username|email` переведены с query-параметров на
      JSON-тело с обязательным `admin_password`, проверяемым
      `verify_password()` перед выполнением — как у уже существующего
      `DELETE /api/v1/users/me` (self-delete), тот же паттерн, не изобретён
      заново.
- [x] Фронтенд `frontend/src/routes/account/admin/+page.svelte` — новая
      страница (НЕ `/admin` — тот маршрут занят экраном ведущего живой игры,
      это разные роли). Таблица заявок и учёток, одобрение, создание,
      модалки смены пароля и удаления с полем «ваш пароль для подтверждения».
- [x] ⚠️ Починено: в `navbar.svelte` ссылка «Регистрация» показывалась по
      условию `{#if registration_disabled}` — то есть при ВЫКЛЮЧЕННОЙ
      регистрации, и пряталась при включённой (перепутанное условие в
      апстриме, подтверждено сверкой с бэкендом `create_user` в
      `classquiz/routers/users/__init__.py`, где логика правильная).
      Исправлено на `{#if !registration_disabled}` в обеих версиях навбара.

### ✅ Проверено вживую 22.09.2026 (реальный Postgres+Redis+Meilisearch+uvicorn, не Docker)

Полный сквозной сценарий через curl, не просто чтение кода:

1. Регистрация → `approved=false` → попытка входа с верным паролем отклонена
   `403 "Учётная запись ожидает подтверждения администратором"`. ✅
2. Первый админ одобрен прямой записью в БД (единственный способ снять
   курицу-яйцо для самой первой учётки — см. пункт ниже про деплой).
3. Одобрение через `POST /admin/users/{id}/approve` → вход тем же паролем
   сразу отдаёт `200` и токен. ✅
4. `POST /admin/users` (создание админом) → `approved=true` сразу, без
   очереди, вход работает немедленно. ✅
5. Сброс пароля: с неверным `admin_password` → `403`, старый пароль
   продолжает работать; с верным `admin_password` → `200`, старый пароль
   перестаёт работать, новый работает. ✅
6. Удаление: с неверным `admin_password` → `403`, учётка на месте; с верным
   → `200`, учётка пропадает из списка. ✅
7. Лимит попыток входа: 8 неверных паролей подряд → 9-я попытка `429` даже
   с **правильным** паролем. ✅

**Найден и исправлен настоящий баг при проверке** (не гипотетический):
`GET /admin/users` и `/admin/users/pending` отдавали **пароль (хеш argon2),
backup-код, TOTP-секрет и аватар целиком** — `response_model_exclude` на
`response_model=list[User]` с ormar-моделью не фильтрует поля (сработало бы
на одиночном объекте, но не на списке). Исправлено на явный белый список
полей `AdminUserOut` (id, email, username, verified, approved, created_at)
вместо чёрного списка — это ещё и надёжнее на будущее: новое чувствительное
поле в `User` само по себе не утечёт, не нужно помнить дополнить исключения.

**Не сделано / нужно учесть при реальном деплое:**
- [ ] `.env`/`docker-compose.yml` — прописать `ADMINS` (username владельца)
      при первом деплое, иначе панель `/account/admin` недоступна никому.
- [ ] **Курица-яйцо первого админа**: сейчас единственный способ выдать
      `approved=true` самой первой учётке — прямой `UPDATE` в базе (так и
      делали при проверке). Задеплоив на VPS, потребуется то же самое —
      либо одноразовый скрипт-бутстрап, либо просто ручной SQL один раз.
- [ ] `docker-compose.yml` тянет `api`/`proxy` как готовые образы апстрима
      (см. Фазу 5) — их нужно собирать из своего кода, иначе все правки
      Фазы 2 не попадут в контейнер.
- [ ] Известный небольшой пробел: `DELETE /user/*` в `admin.py` не чистит
      Meilisearch-индекс публичных квизов удаляемого пользователя (в отличие
      от self-delete `/users/me`, который это делает) — для закрытой
      college-платформы без публичного поиска это низкий приоритет, но
      стоит знать.
- [ ] Аудит-лог действий админа (кто кого одобрил/удалил/когда) — не
      делали, в апстриме такого нет. Стоит добавить отдельной строкой перед
      реальным вводом в эксплуатацию — сейчас это стандартный элемент
      "профессиональной" админ-панели, которого не хватает.
- [ ] `{"deleted":null}` в ответе DELETE-эндпоинтов — `User.objects.delete()`
      в этой версии ormar не возвращает число удалённых строк (унаследовано
      от апстрима), ответ неинформативен. Не баг, но стоит улучшить.
- [ ] Фронтенд-страница `/account/admin` написана, но не открывалась в
      браузере в этой сессии (только бэкенд гонялся через curl) — визуально
      не проверена.

## Безопасность — что сделано и что реально защищает, а не украшает

Пользователь 22.09.2026 попросил "чтоб F12 не работала, чтоб взломать
было нельзя, чтоб данные в зашифрованном виде хранились". Разобрано так:

- **Блокировка DevTools (F12) — сознательно НЕ делаем.** Обходится тривиально
  (другой браузер, `curl`, прямой запрос к API), не останавливает никого,
  а мешает легитимной отладке и доступности. Профессиональные аудиторы
  считают это анти-паттерном ("security theater"). Настоящая защита —
  в самом API, не в браузере.
- **Пароли** — уже хранятся хешем **argon2** (`classquiz/auth.py`,
  `pwd_context = argon2` из passlib) — это современный рекомендуемый
  алгоритм, необратимый даже для нас самих. Менять не на что, это уже
  правильно сделано в апстриме.
- **Подбор пароля** — добавлено ограничение попыток входа (Фаза 2,
  `classquiz/routers/login.py`): 8 неудачных попыток на учётку → блок на
  15 минут, считается в Redis. Раньше пароль можно было перебирать сколько
  угодно раз.
- **Права администратора** — переведены с "первой строки в базе" на явный
  список `ADMINS` (см. Фазу 2) — раньше это был скрытый и непроверяемый
  механизм.
- **Удаление/смена пароля учётки** — только с повторным вводом пароля
  самого админа (см. Фазу 2).
- **Cookies сессии** — уже `httponly` + `samesite=lax` в апстриме
  (`classquiz/oauth/authenticate_user.py`) — то есть токен нельзя прочитать
  из JS (защита от кражи через XSS) и запрос с чужого сайта его не пошлёт
  (защита от части CSRF-сценариев). Тоже уже сделано правильно, не трогали.
- **Передача данных** — HTTPS обеспечивает `proxy`/Caddy из
  `docker-compose.yml` (автоматический сертификат) — доделывается в Фазе 5.
- **Данные "в зашифрованном виде"** — для базы Postgres и загруженных файлов
  правильный способ — **шифрование диска/тома на самом VPS** (провайдер
  почти всегда даёт encrypted volume, либо LUKS вручную), а не шифрование
  каждого поля в приложении. Прикладное шифрование всех данных ломает
  поиск и фильтрацию и не то, что делают "обычные программы" — они
  полагаются на шифрование диска + шифрование трафика + хеширование
  паролей, что мы и делаем. **Действие для Фазы 5**: включить encrypted
  volume у провайдера VPS при заказе сервера.
- **Не сделано, стоит добавить перед реальным вводом в эксплуатацию**:
  - rate-limit на само создание учёток (`POST /users/create`) — сейчас
    ограничен только повторный вход, не сама регистрация (спам-регистрации
    не заблокировать, хотя они всё равно попадут в очередь одобрения);
  - security-заголовки на уровне Caddy (`Content-Security-Policy`,
    `X-Frame-Options`, `Referrer-Policy`, `Strict-Transport-Security`);
  - `SECRET_KEY` в `docker-compose.yml` сейчас плейсхолдер `"TOP_SECRET"` —
    сгенерировать реальный (`openssl rand -hex 32`) при деплое, никогда не
    коммитить;
  - Dependabot у форка (унаследовал открытые PR апстрима на момент форка —
    `git branch -a` показывает ветки `dependabot/*`) — включить алерты в
    Settings репозитория `kst-platform/ClassQuiz`, чтобы получать патчи
    уязвимостей зависимостей автоматически.

## Фаза 3 — группы — ✅ реализовано и проверено вживую 22.09.2026

- [x] `classquiz/db/models.py`: `Group` (id, name, teacher → User,
      created_at), `GroupMember` (id, group → Group, full_name). Миграция
      `a680fa3e11df_added_groups.py`.
- [x] `PlayGame` (Redis-модель живой игры) дополнена `group_id` и `roster:
      list[str] | None` — ростер копируется из БД один раз при
      `/quiz/start`, чтобы `join_game` не ходил в Postgres на каждое
      подключение (тот же принцип, что и остальное состояние живой игры).
- [x] `classquiz/routers/groups.py` — CRUD групп и ростера, всё
      teacher-scoped (`Group.objects.get_or_none(id=.., teacher=user)` —
      чужую группу не видно и не через прямой id). Массовое добавление
      студентов — одним текстом, по строке на ФИО, с дедупликацией.
- [x] `classquiz/routers/quiz.py` `start_quiz` — новый параметр `group_id`,
      проверяет владельца, отказывает, если в группе 0 студентов.
- [x] `classquiz/socket_server/__init__.py` `join_game` — если у игры есть
      `roster`, имя, которого там нет, отклоняется событием
      `username_not_in_roster` (до проверки капчи, до защиты от дублей).
- [x] Фронтенд: `frontend/src/routes/dashboard/groups/+page.svelte` (список
      групп, создание, вставка ФИО текстом, переименование, удаление),
      `start_game.svelte` (выбор группы при запуске игры),
      `play/join.svelte` (если сервер прислал `roster` — вместо поля ввода
      ника выпадающий список имён, плюс обработка `username_not_in_roster`).

**Проверено вживую** (тот же стенд — Postgres/Redis/Meilisearch/uvicorn,
плюс реальный Socket.IO-клиент на Python для проверки `join_game`, не
просто HTTP): создание группы → массовое добавление 3 ФИО с одним
дублем → дубль не добавился, 3 уникальных сохранены → чужой преподаватель
получает `404` на чужую группу и пустой список своих → создан квиз →
игра запущена с `group_id` → `roster` пришёл в ответе старта и в
`/quiz/play/check_captcha` → **реальное WebSocket/polling-подключение**:
имя не из списка → `username_not_in_roster`, имя из списка → `joined_game`,
повторный вход тем же именем со второго подключения → `username_already_exists`
(старая защита от дублей продолжает работать вместе с новой проверкой
ростера) → удаление группы каскадно удалило её студентов из БД.

**Грабли этого захода (не в коде платформы, в тестовом харнессе)**: при
передаче кириллицы через `curl -d "..."` в Git Bash на Windows консоль
молча заменяет буквы на `?` (байт `0x3F`) ещё до отправки запроса — из-за
этого два разных ФИО, начинающихся с русских букв одинаковой длины,
превращались в идентичную строку и один "терялся" при дедупликации. Это
не баг сервера — проверено сверкой с сырыми байтами в Postgres. Лечится
передачей JSON через файл (`curl --data-binary @файл.json`), а не строкой
в `-d`. Реальный фронтенд (SvelteKit `fetch`) всегда кодирует тело в UTF-8
и этой проблемы не имеет.

**Не сделано / стоит учесть:**
- [ ] `classquiz/routers/results.py` — экспорт результатов пока не помечает
      игрока как «из группы N»/ФИО из ростера отдельно от обычного
      username; сейчас username и есть выбранное ФИО, этого може хватить,
      но явной пометки «сессия была групповой» в выгрузке нет.
- [ ] Фронтенд-страницы (`dashboard/groups`, обновлённые `start_game.svelte`
      и `join.svelte`) не открывались в браузере — только бэкенд и сокет
      проверялись напрямую. Визуально не проверено.
- [ ] Если преподаватель удалит группу посреди уже идущей игры — `roster`
      уже скопирован в Redis при старте, текущая игра не пострадает, но
      это стоит явно знать (не автотестировалось).

## Фаза 4 — импорт GIFT — ⬜ не начато
См. план: `classquiz/gift_importer/` по образцу `kahoot_importer/`, логика
парсинга переносится из `C:\Users\Dmitry\Projects\_tools\kt\gift.py`.

## Фаза 5 — деплой на VPS — ⬜ не начато, ждём сервер
`docker-compose.yml` в репозитории тянет `api` и `proxy` как готовые образы
`ghcr.io/mawoka-myblock/classquiz-*:master` — **это нужно поменять на сборку
из своего кода** (как уже сделано для `frontend`), иначе бэкенд-правки
Фаз 2–4 не попадут в контейнер. Иначе конфигурация (`.env`, Caddy) готова
использоваться почти как есть.
